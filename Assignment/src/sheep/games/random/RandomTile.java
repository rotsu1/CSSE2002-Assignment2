package sheep.games.random;

/**
 * An interface which produces a random number.
 */
public interface RandomTile {

    /**
     * Returns a random integer.
     * @return A random integer.
     */
    int pick();
}
